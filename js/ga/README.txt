These JavaScript files are copied completely from Google Analytics hosting system because Manifest v3 doesn't allow external scripts anymore, regardless of CSP settings.

And, even with an appropriate `integrity="..."` hash on the inline script, it also will not run.
