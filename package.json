{
"name": "learn-metadata-tool",
  "version": "0.7.11",
  "description": "Helping you maintain content on Microsoft Learn and Docs. Extract useful metadata quickly for editing or triage purposes.",
  "scripts": {
    "build": "webpack",
    "postbuild": "cp manifest.json dist/ && mkdir -p dist/icons && cp images/learn-tool*.png dist/icons/ && cp *.html dist/ && cp *.css dist/",
    "test": "jest"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.316",
    "@types/jest": "^29.5.14",
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^29.7.0",
    "jsdom": "^26.1.0",
    "ts-jest": "^29.3.2",
    "typescript": "^5.0.0",
    "webpack": "^5.0.0",
    "webpack-cli": "^5.0.0",
    "ts-loader": "^9.0.0"
  }
}
